#include <iostream.h>
#include <conio.h>

int main()
{
    int num1, num2;
    cout << "Enter the first number: ";
    cin >> num1;
    cout << "Enter the second number: ";
    cin >> num2;

    int sum = num1 + num2;
    int Sub = num1 - num2;
    int Multi = num1 * num2;
    int Div = num1 / num2;
    int Modulo = num1 % num2;

    cout << "Sum: " << sum << endl;
    cout << "Sub: " << difference << endl;
    cout << "Multi: " << product << endl;
    cout << "Div: " << quotient << endl;
    cout << "Modulo: " << remainder << endl;

    getch();
    return 0;
}
